stages:
  - run
  # - test
  # - deploy

# cache:
#   key: ${CI_COMMIT_REF_SLUG}
#   paths:
#     - node_modules/

# variables: 
#   STAGING_DOMAIN: instazone-staging.surge.sh
#   PRODUCTION_DOMAIN: instazone.surge.sh
#   #se usan onda: http://$STAGING_DOMAIN
#   # http://$STAGING_DOMAIN
#   # http://$STAGING_DOMAIN.sarasa
#   # Se pueden usar para, por ejemplo, environments dinámicos

# # .build website: #con el punto se anula el stage
run:
  stage: run
  image: python
  # only: #solo correrlo en la rama:
  #   - master 
  script:
    - pip3 install keyboard
    - pip3 install pyqt6
    - apt-get update && apt-get install -y libgl1
    - sudo apt-get install libxkbcommon-x11-0
    - python main.py
    # - npm install
    # - npm install -g gatsby-cli
    # - gatsby build
  # artifacts:
  #   paths:
  #     - ./public
